<h1 class="text-2xl font-bold">👑 Your Groups</h1>

<%= link_to "➕ Create New Group", new_group_path, class: "text-blue-500" %>

<% if @groups.any? %>
  <ul class="mt-4">
    <% @groups.each do |group| %>
      <li class="mb-2 border p-2 rounded flex justify-between">
        <strong><%= link_to group.name, group_path(group) %></strong>
        <span class="text-sm text-gray-500">(<%= group.members.count %> members)</span>
        <span class="text-sm text-gray-500">(Created on <%= group.created_at.strftime("%b %d, %Y") %>)</span>
        
        <div class="mt-1">
          <%= link_to "✏️ Edit", edit_group_path(group), class: "text-blue-500 mr-2" %>
          <%= link_to "❌ Delete", group_path(group), method: :delete, data: { confirm: "Are you sure?" }, class: "text-red-500" %>
        </div>
      </li>
    <% end %>
  </ul>
<% else %>
  <p class="mt-4">You haven't created any groups yet.</p>
<% end %>
